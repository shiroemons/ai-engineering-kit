# HTTP Client 調査テンプレート

未調査。検索・検証の対象外。[共通の調査・昇格手順](../../docs/workflows.md) を使う。

接続・読み取り・全体 timeout をどう区別するか。context、接続再利用、body の解放、応答サイズ、redirect、retry の責務を誰が持つか。失敗を記録するログとメトリクスを確認する。

根拠の commit・パス、採用理由、代替案、資源上限を記録する。
