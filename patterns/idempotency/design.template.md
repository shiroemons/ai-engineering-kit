# Idempotency 調査テンプレート

未調査。検索・検証の対象外。[共通の調査・昇格手順](../../docs/workflows.md) を使う。

同一キー・同一 payload の再送を何として扱うか。同一キーで異なる payload はどう拒否するか。保存と実行の原子性、並行要求、保持期限、失敗後の retry をどのテストで確認するか。

根拠の commit・パス、採用理由、代替案、運用上の代償を記録してから通常の `.md` へ昇格する。
