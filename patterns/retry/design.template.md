# Retry 調査テンプレート

未調査。検索・検証の対象外。[共通の調査・昇格手順](../../docs/workflows.md) を使う。

どの失敗を再試行するか。操作の冪等性は何で保証するか。回数・総時間・backoff・jitter・Retry-After・キャンセルの責務をどこに置くか。失敗の継続をどのメトリクスで検知するか。

根拠の commit・パス、採用理由、代替案、再試行による負荷を記録する。
